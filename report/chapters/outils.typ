= Outils utilisés <tools>

Unity dispose de 3 pipelines de rendus graphique.
- Standard - built-in est celle par défaut et est une option legacy que les deux suivantes tentent de remplacer depuis plusieurs années
- URP - Universal Render Pipeline est un rendu visant la performance afi de couvrir le grand marché mobile (~50% du marché)
- HDRP - HD Render Pipeline est un rendu visant la qualité graphique. Les plate-formes vouées pour de tels projets sont celles PC et consoles.

Linear vs Gamma color space -> visual detail -> skip @lod-3d-graphics

impostor : pixyz -> unity industry
plugins : amplify